{
  "name": "sakary-cafe",
  "version": "1.0.0",
  "private": true,
  "description": "حساب القهوة — MERN café tables & orders (Vercel monorepo)",
  "scripts": {
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "seed": "npm --prefix server run seed"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.5.1",
    "morgan": "^1.10.0"
  }
}
